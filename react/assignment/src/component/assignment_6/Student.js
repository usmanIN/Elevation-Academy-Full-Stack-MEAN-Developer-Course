import React from "react";
import { AddStudent, UpdateStudent } from "./Action";
import {useDispatch,useSelector} from 'react-redux';

const StudentList = () =>{
    
    //const myState = useSelector((state) => state.handleAction);
    
    return(
        <>
        
        </>        
    )
}
export default StudentList